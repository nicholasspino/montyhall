% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/monty-hall-pkg-Spino.R
\name{play_game}
\alias{play_game}
\title{Play One Game with Both Strategies}
\usage{
play_game()
}
\value{
A data frame with two rows and two character columns:
  strategy ("stay", "switch") and outcome ("WIN" or "LOSE").
}
\description{
Play a three-door game and record stay and switch outcomes.
}
\details{
No arguments are required. Both strategies use the same game,
  initial selection, and opened door. Exactly one strategy wins each game.
}
\examples{
result <- play_game()
result
stopifnot(identical(result$strategy, c("stay", "switch")),
          sum(result$outcome == "WIN") == 1,
          sum(result$outcome == "LOSE") == 1)
}
